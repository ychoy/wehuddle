<%= form_for @chatroom, html: { multipart: true }, id: "new_chatroom" do |f| %>
			<%= f.hidden_field :admin, value: current_user.id %>
      <%= f.label :title %>
      <%= f.text_field :title %>

      <%= f.label :description %>
      <%= f.text_area :description %><br>

      <%= f.submit yield(:button_text), class: "btn btn-default" %>
<% end %>
